/* @title: Malformed Idea */
/* NOTE: this file intentionally omits the `@idea:small ...` signature line,
 * so the current parser silently drops the idea below. Phase 5 makes this a
 * loud warning instead of a silent drop. */
/* # Small Idea: Orphaned Body (EXPERIMENTAL)
 *
 * ## Credits: axvon
 *
 * ## Problem:
 *   The signature comment is missing, so nothing ingests this.
 */
#ifndef FIXTURE_IDEA_BAD_H
#define FIXTURE_IDEA_BAD_H
#endif
